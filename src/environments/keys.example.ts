// Modelo de src/environments/keys.ts (que é ignorado pelo git).
// Copie este arquivo para keys.ts e preencha com a sua chave:
//   cp src/environments/keys.example.ts src/environments/keys.ts
//
// A chave de navegador do Google Maps é pública por natureza, mas deve ser
// restrita por referrer no Google Cloud. A chave da Solar API NUNCA entra
// no frontend — quem fala com a Solar API é o backend.
export const googleMapsBrowserKey = '';
