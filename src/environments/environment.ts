import { googleMapsBrowserKey } from './keys';

export const environment = {
  production: true,
  // URL do backend no Railway — ajustar quando o serviço for provisionado.
  apiBaseUrl: 'https://curazi-api.up.railway.app',
  googleMapsBrowserKey,
};
